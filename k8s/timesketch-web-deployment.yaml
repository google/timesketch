apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    io.kompose.service: timesketch-web
  name: timesketch-web
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: timesketch-web
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml
        kompose.version: 1.26.0 (40646f47)
      creationTimestamp: null
      labels:
        io.kompose.service: timesketch-web
    spec:
      containers:
        - args:
            - timesketch-web
          env:
            - name: NUM_WSGI_WORKERS
          image: 'us-docker.pkg.dev/osdfir-registry/timesketch/timesketch:'
          name: timesketch-web
          resources: {}
          volumeMounts:
            - mountPath: /etc/timesketch/
              name: timesketch-web-claim0
            - mountPath: /usr/share/timesketch/upload/
              name: timesketch-web-claim1
            - mountPath: /var/log/timesketch/
              name: timesketch-web-claim2
      restartPolicy: Always
      volumes:
        - name: timesketch-web-claim0
          persistentVolumeClaim:
            claimName: timesketch-web-claim0
        - name: timesketch-web-claim1
          persistentVolumeClaim:
            claimName: timesketch-web-claim1
        - name: timesketch-web-claim2
          persistentVolumeClaim:
            claimName: timesketch-web-claim2
status: {}
